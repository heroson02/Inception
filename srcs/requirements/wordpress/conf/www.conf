[www]

user = www-data
group = www-data

# listen = wordpress:9000
listen = /var/run/php7.3-fpm.sock

pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
clear_env = no